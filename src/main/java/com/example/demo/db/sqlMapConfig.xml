<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="com/example/demo/db/db.properties"/>
	<typeAliases>	<!-- type경로가 너무 기니까 애칭 설정-->
		<typeAlias type="com.example.demo.vo.UserVO" alias="userVO"/>
	</typeAliases>
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>
  </environments>
  <mappers> <!-- 맵핑파일 목록 등록 => bookmapper 파일 우클릭=> 프로퍼티스에서 경로복사 -->
    <mapper resource="com/example/demo/db/teacherMapper.xml"/>
  </mappers>
</configuration>